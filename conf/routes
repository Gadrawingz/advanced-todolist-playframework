# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                       controllers.Application.index()

# About
GET     /about                  controllers.Application.aboutUs()

# Contact
GET     /contact                controllers.Application.contactUs()

# Support
GET     /support                controllers.Application.support()

# User Login
GET     /login                  controllers.Application.userLogin()

# Signup
GET     /signup                 controllers.Application.userSignup()



# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
